
# 请求链路与契约测试

## 请求链路

1. 请求进入 FastAPI 路由。
2. 依赖层解析当前用户。
3. 路由调用 Service。
4. Service 调 ORM / 外部 AI / OSS。
5. 返回若依风格 `AjaxResult` 或分页结构。

## 契约测试建议

用 `docs/route_inventory.json` 作为测试基线：

- 断言路由都存在。
- 断言 HTTP 方法都一致。
- 对关键接口做请求/响应快照对比。
