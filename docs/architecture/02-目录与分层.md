
# 目录与分层

```text
app/
├── api/           # 路由层
├── core/          # 基础设施
├── models/        # ORM
├── schemas/       # DTO / VO
└── services/      # 业务逻辑
```

## 约束

- 路由层不要直接写 SQL。
- 服务层不要直接读环境变量。
- ORM 不要混进 HTTP 逻辑。
- 契约测试不要依赖前端代码。
