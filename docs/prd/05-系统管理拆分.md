
# 系统管理拆分

## 1. 用户

当前骨架已放入：

- 用户列表、详情、新增、修改、删除、启停、重置密码
- 分配角色
- 个人资料、改密、头像
- Excel 导出

## 2. 角色

当前骨架已放入：

- 角色列表、详情、新增、修改、删除、启停
- 分配用户五件套
- Excel 导出

## 3. 菜单

当前骨架已放入：

- 菜单列表、详情、新增、修改、删除
- `treeselect`
- `roleMenuTreeselect`

## 4. 权限实现建议

当前骨架先把路由和依赖结构预留好。

下一步应做：

1. 从 Java 端提取所有 `permission` 字符串。
2. 建立 Python 端权限矩阵。
3. 对 `/system/*` 补 `require_permission(...)`。
