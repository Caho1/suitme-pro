
# 测试与切换

## 1. 契约测试

必须同时做三类：

- JSON 契约测试
- 文件接口契约测试
- 权限边界测试

## 2. 推荐切换方式

### 方式 A：同库整包切换

适合你现在这种“前端只改 baseUrl”的目标。

### 方式 B：网关灰度切流

如果你后续想降低上线风险，可以用 Nginx 按路径逐步切流。

## 3. 回滚策略

如果数据库不改，只切后端服务，那么最简单的回滚就是把 baseUrl 或网关路由切回 Java 服务。
